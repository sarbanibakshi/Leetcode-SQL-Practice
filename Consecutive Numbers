-- ============================================
-- Problem: Consecutive Numbers
-- ============================================

-- Table: Logs
-- +-------------+---------+
-- | Column Name | Type    |
-- +-------------+---------+
-- | id          | int     |
-- | num         | varchar |
-- +-------------+---------+
--
-- id is the primary key and auto-incremented.

-- --------------------------------------------
-- Objective:
-- Find numbers that appear at least three times consecutively.
-- --------------------------------------------

-- Solution:
SELECT DISTINCT l1.num AS ConsecutiveNums
FROM Logs l1
JOIN Logs l2
  ON l1.id = l2.id - 1
JOIN Logs l3
  ON l2.id = l3.id - 1
WHERE l1.num = l2.num
  AND l2.num = l3.num;

-- --------------------------------------------
-- Approach:
-- - Joined table with itself to compare consecutive rows
-- - Ensured ids are consecutive (id, id+1, id+2)
-- - Checked if all three nums are equal
-- - Used DISTINCT to avoid duplicates
-- --------------------------------------------
